const PagesToReadError = () => {
    return (
        <div className="flex justify-center items-center mt-12">
            <h4 className="text-xl font-semibold">Sorry Currently no books are added in the ReadList..</h4>
        </div>
    );
};

export default PagesToReadError;